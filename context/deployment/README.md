# Deployment

Operational deployment docs for PrepAhead (Vercel, branch `prod`, production at prepahead.dev).

**Canonical workflow:** [`deploy-plan.md`](deploy-plan.md) — PR into `prod`, Preview on PR, merge, production updates the custom domain.

Per-change deployment notes (if any) can be added here as `deploy-plan-<change-id>.md` or logged in the execution table inside `deploy-plan.md`.
